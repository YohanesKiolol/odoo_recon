name: Build Windows EXE

on:
  push:
    branches: [main]
  workflow_dispatch:   # allow manual trigger from GitHub UI

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Create empty input / output folders
        run: |
          New-Item -ItemType Directory -Force -Path input
          New-Item -ItemType Directory -Force -Path output
        shell: pwsh

      - name: Build EXE with PyInstaller
        run: pyinstaller bank_recon.spec

      - name: Prepare release package
        run: |
          New-Item -ItemType Directory -Force -Path dist\input
          New-Item -ItemType Directory -Force -Path dist\output
          Copy-Item .env.example dist\.env.example
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BankRekonsiliasi-Windows
          path: dist\
          retention-days: 30
